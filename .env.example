# Registration – set to "true" to show the registration form; omit or "false" to show closed banner
# NEXT_PUBLIC_REGISTRATION_OPEN=true

# Supabase – required for the app
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: for server-side admin actions (e.g. bypass RLS)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Direct DB connection (optional – for psql, migrations, or tools)
# Get from: Supabase Dashboard → Project Settings → Database → Connection string (URI)
# SUPABASE_DB_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
